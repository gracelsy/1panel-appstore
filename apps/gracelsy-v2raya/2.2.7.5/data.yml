additionalProperties:
  formFields:
    - default: 2017
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      label:
        zh: WebUI 端口
        en: WebUI Port
      required: true
      rule: paramPort
      type: number

    - default: 20170
      edit: true
      envKey: SOCKS5_PROXY_PORT
      label:
        zh: Socks5 代理端口
        en: Socks5 Proxy Port
      required: true
      rule: paramPort
      type: number

    - default: 20171
      edit: true
      envKey: HTTP_PROXY_PORT
      label:
        zh: HTTP 代理端口
        en: HTTP Proxy Port
      required: true
      rule: paramPort
      type: number

    - default: 20172
      edit: true
      envKey: HTTP_SHUNT_PROXY_PORT
      label:
        zh: HTTP 代理端口(带分流规则)
        en: HTTP Port(Usage Shunt Rule)
      required: true
      rule: paramPort
      type: number

    - default: "Asia/Shanghai"
      edit: true
      envKey: TIME_ZONE
      label:
        zh: 时区
        en: Time Zone
      required: true
      type: text
